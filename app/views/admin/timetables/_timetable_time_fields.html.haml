.nested-fields
  = f.label :time
  = f.time_select :time, { with_css_classes: true }, { class: 'select' }
  
  %h3
    Stops
    
  .stops
  
  %p
    = link_to_remove_association 'Remove time', f

  %hr
