<div class="container">
  <div class="inner">
    <div class="card no-click">
      <div class="inner">
        <%= form_for [:admin, @vehicle] do |form| %>
          <%= render partial: 'fields', locals: {form: form} %>
          
      </div>
      <%= form.submit "Create Vehicle", :class => "button border-bottom-radius" %>
      <% end %>
    </div>
  </div>
</div>