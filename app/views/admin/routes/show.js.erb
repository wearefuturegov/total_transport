$('#time-<%= @time_id %> .stops').html('<%= j(render partial: 'stop', collection: params[:reversed] == 'true' ? @route.stops.reverse : @route.stops, locals: { selected: @route.stops.collect(&:name), time_id: @time_id }) %>');
