<% if @possible_bookings %>
  $('#results').html('<%= j(render 'possible_bookings', bookings: @possible_bookings, from: @from, to: @to) %>');
<% else %>
  $('#results').html('<%= j(render 'bookings_form', bookings: @bookings, suggestion: false) %>');
<% end %>
