%div{"aria-live" => "assertive", :role => "alert"}
  %p.error-block
    = t('message.sorry')
    to <strong>#{to.name}</strong>.
  %p
    = t('message.suggest')
    <strong>#{from.name}</strong>
    = t('message.suggest_end')

  %h3
    = from.name
    %span
      to
    = fa_icon 'map-marker'
  - bookings.each do |bookings|
    .single
      .details
        = render 'bookings_form', bookings: bookings, suggestion: true
